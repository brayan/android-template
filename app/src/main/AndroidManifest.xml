<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sailboat.template">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.LauncherActivity"
            android:theme="@style/AppTheme.SplashScreen">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.drawer.MainDrawerActivity"
            android:theme="@style/AppTheme.Drawer" />

        <activity android:name=".ui.main.bottom.MainBottomActivity" />

        <activity android:name=".ui.main.tabs.MainTabActivity" />

        <activity android:name=".ui.entity.list.EntityListActivity" />

        <activity android:name=".ui.entity.details.EntityDetailsActivity" />

        <activity
            android:name=".ui.entity.insert.EntityInsertActivity"
            android:windowSoftInputMode="stateVisible" />

    </application>

</manifest>